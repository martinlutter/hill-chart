/**
 * Minimal MV3 service worker.
 * No background logic needed yet — this file satisfies the Chrome MV3 requirement.
 * Future: browser action click handler, context menu, cross-tab state via chrome.storage.session
 */

// Required for Vite to treat this as an ES module
export {}
