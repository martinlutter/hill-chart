services:
  e2e:
    image: mcr.microsoft.com/playwright:v1.58.2-noble
    working_dir: /app
    volumes:
      # Mount source — changes are reflected immediately
      - .:/app
      # Separate named volume for node_modules to avoid host/container platform conflicts
      - e2e_node_modules:/app/node_modules
    command: tail -f /dev/null # Keep container running for interactive use
    # Required for Chrome's shared memory usage inside Docker
    ipc: host

volumes:
  e2e_node_modules:
